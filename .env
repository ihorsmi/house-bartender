# Server
ADDR=:8080
BASE_URL=http://localhost:8080

# Storage (inside container)
DATA_DIR=/data
DB_PATH=/data/housebartender.db
UPLOAD_DIR=/data/uploads

# Session signing keys (recommended: set these to stable random values)
# Generate 32 bytes (64 hex chars) e.g.:
#   python -c "import os,binascii; print(binascii.hexlify(os.urandom(32)).decode())"
SESSION_HASH_KEY_HEX=
SESSION_BLOCK_KEY_HEX=

# First-run admin bootstrap (optional).
# If no admin exists and ALL 3 are set, the app will create the admin automatically.
# Otherwise it will show /onboarding (only if no admin exists).
BOOTSTRAP_ADMIN_EMAIL=admin@local
BOOTSTRAP_ADMIN_PASSWORD=admin1234
BOOTSTRAP_ADMIN_NAME=admin
